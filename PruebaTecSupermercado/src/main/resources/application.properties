spring.application.name=pruebatecsupermercado
# ==========================
# Configuraciones de JPA + MySQL + docker
# ==========================
#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USER_NAME}
#spring.datasource.password=${DB_PASSWORD}
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.dialect=${DB_PLATFORM}
#server.port=8080

# ==========================
# Configuraciones de JPA + MySQL
# ==========================
spring.datasource.url=jdbc:mysql://localhost:3307/pruebatecsuper
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
